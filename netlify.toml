# Netlify deploy config for the Lodgedwell Astro site
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Astro outputs clean-URL directories; Netlify serves them automatically.
