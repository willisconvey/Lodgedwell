---
name: lodgedwell-design
description: Design anything for Lodgedwell (online conveyancing, Victoria) so it matches lodgedwell.com.au exactly — tokens, components, logo, voice.
user-invocable: true
---

Read `readme.md` here first, then link `styles.css` and use its classes
(`.btn`, `.btn--coral`, `.card`, `.price-card`, `.pill`, `.tabs`, `.site-header`,
`.site-footer`, …) rather than restyling from scratch. Use the SVGs in `assets/`
for the logo — never redraw it. Keep copy in sentence case, no emoji, and never
invent ratings, counts or testimonials. Pricing and contact facts are in
`readme.md`. Production source of truth is the `lodgedwell-astro` repo.
